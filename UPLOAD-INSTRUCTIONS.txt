UPLOAD BOTH FILES TO THE ROOT OF THE REPOSITORY:

1. app.js
2. service-worker.js

Choose “Replace” for both files and commit the changes.

The previous update did not reach the repository: the live app.js is still the old version.
The service worker also cached that old file under vanguard-v21.

This package replaces the JavaScript and changes the cache to vanguard-v22-footer-vpin,
which forces GitHub Pages to load the updated footer and VPIN text.

After GitHub finishes deploying:
- Close the site tab completely and reopen it.
- If needed, refresh once more after 1–2 minutes.
